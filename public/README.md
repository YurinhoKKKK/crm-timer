# public/ — arquivos estáticos

Coloque aqui as duas versões da logo, com **exatamente** estes nomes:

- `public/logo-azul.png` — logo AZUL (fundo transparente), usada em fundos
  claros (ex.: tela de login no tema claro).
- `public/logo-branca.png` — logo BRANCA (fundo transparente), usada em fundos
  escuros e na sidebar colorida (ex.: sidebar, login no tema escuro).

O código já referencia esses caminhos via `src/components/Logo.tsx`. Mantenha
os nomes idênticos (minúsculas, com hífen) para a logo aparecer.
